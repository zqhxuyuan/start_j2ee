Quartz静态配置调度任务(内存方式)-->SimpleTrigger/CronTriggerQuartz1+Spring简单集成	调度器Scheduler:	由一系列的触发器构成	触发器Trigger:	任务实例+任务表达式	任务实例JobDetail:		JobDetailBean.业务类要继承QuartzJobBean类.			业务实现类Service如果依赖Dao层(和数据库有操作),			可以在map标签里放入多个Dao;或者放入成员变量		MethodInvokingJobDetailFactoryBean.不必实现相关类.			方法调用的JobDetailBean.适用于简单的任务			如果Service依赖Dao等实现.不能使用: MethodInvokingJobDetailFactoryBeanSimpleTrigger简单触发器:	startDelay:在服务器启动好10秒(延迟)后启动,相当于设置了开始时间.当然也可以指定某个时间点启动	不设置重复次数,报错:org.quartz.SchedulerException: Repeat Interval cannot be zero.	repeatInterval:每次执行的间隔	repeatCount:执行次数.	repeatCount=1,执行2次,每次间隔1000ms. 	(重复次数>0,间隔也必须>0)		<property name="repeatInterval" value="10000"/>        <property name="repeatCount" value="1"/>	repeatCount=0,repeatInterval=0			重复次数0,间隔0.只执行一次		<property name="repeatInterval" value="0"/>        <property name="repeatCount" value="0"/>持久化调度任务的状态(数据库方式集成 Quartz)