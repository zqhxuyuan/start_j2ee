package com.xuyuan.ssh.web;import org.apache.struts2.convention.annotation.Result;import org.apache.struts2.convention.annotation.Results;/** * com.xuyuan.ssh.web.WelcomeAction * 所以要访问到该类的execute(),访问的URL请求为:/welcome.action.	[见index.jsp] * 执行该方法后,返回"success",则根据命名规则,默认页面位置为:WEB-INF/content/welcome.jsp * 但是在Action上的注解@Results表示会修改配置，页面将跳转到login/login-index.jsp *  * 如果要访问该类的别的方法,比如index(),则访问的URL请求为:/welcome!index.action * 如果方法返回的是其他字符串比如"index",则页面为:WEB-INF/content/welcome-index.jsp *  * 最简单的配置就是没有配置(嘻嘻),那么按照默认的命名规则是不错的选择喽. *  * 2012-12-27 update: 统一入口页面为login/login-index.jsp * @author Administrator */@Results({	@Result(name="success",location="login/login-index.jsp")})public class WelcomeAction {	public String execute(){		return "success";	}		public String index(){		return "index";	}}