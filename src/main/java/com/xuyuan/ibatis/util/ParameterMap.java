package com.xuyuan.ibatis.util;import java.util.HashMap;  /** *  * 扩展了HashMap的类，只有一个显式定义的构造方法，并且使用了可变参数，方法处理是将输入的参数按照一定规则存入HashMap。 * 规则:按照参数列表中的“名”-“值”对来放置的。使用了可变参数后，那么参数想放几对就放几对，非常方便。 * 为了能在XML中使用这个类型，不要忘了声明它。 * @author http://sarin.iteye.com/blog/715360 * */public class ParameterMap extends HashMap<Object, Object> {    private static final long serialVersionUID = 1L;        public ParameterMap(Object... parameters) {          for (int i = 0; i < parameters.length - 1; i += 2) {              super.put(parameters[i], parameters[i + 1]);          }      }  }  